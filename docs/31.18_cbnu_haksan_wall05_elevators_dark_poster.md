# CBNU 학연산 Wall 05 엘리베이터 철문 및 통형 포스터 수정 기록

- 수정 날짜: 2026-09-04 (Asia/Seoul)
- 기준 월드: `worlds/cbnu_haksan_1f_corridor/cbnu_haksan_1f_corridor.usda`
- 목적: 통형 포스터 코너를 돌아 보이는 빈 벽을 실제 운영 중인 엘리베이터 구역으로 표현하고, 통형 포스터의 색상과 두께를 보강한다.

## 엘리베이터 철문

- 대상 벽: 북측 복도 서쪽의 `Wall_05`
- 새 Prim: `/World/Architecture/ElevatorDoor_01`, `/World/Architecture/ElevatorDoor_02`
- 위치: `(22.8392, 15.2, 0.0)`, `(22.8392, 19.0, 0.0)`
- 크기: 각 `1.45 × 2.30 × 0.08m`
- 형태: 중앙개폐식 스테인리스 철문 2장, 금속 프레임과 점등 상태 표시기
- 상태 metadata: `operational`, `closed`
- 두 문은 기존 북측 복도 조명 축과 정렬했다.
- 별도 collision은 만들지 않고 기존 `Wall_05` collider를 유지했다.

## 통형 포스터

- 대상 Prim: `/World/Architecture/GrayPoster_02`
- 색상: `(0.70, 0.72, 0.74)`에서 `(0.34, 0.36, 0.38)` 진회색으로 변경
- 두께: `0.075m`에서 `0.15m`로 정확히 2배 증가
- 가로 `4.5m`, 세로 `1.22m`, 위치와 단일 `PosterSlab` 구조는 유지했다.
- screen, bezel, emission, collider는 추가하지 않았다.

## 검증

```bash
cd /home/a/Isaac_Worlds
python3 scripts/update_cbnu_haksan_architecture.py
MPLCONFIGDIR=/tmp/cbnu_matplotlib python3 scripts/render_cbnu_haksan_preview.py
python3 scripts/validate_cbnu_haksan_detail.py
./scripts/test_world_with_isaac_usd.sh
```

- 상세 정적 검사: PASS
- Isaac Sim USD 24.05 composed Stage 검사: PASS
- `Wall_05`의 두 엘리베이터 문과 각 좌·우 철문 패널, 점등 표시기 로드를 확인했다.
- 통형 포스터가 `(0.34, 0.36, 0.38)`, `4.5 × 1.22 × 0.15m`의 단일 비발광 슬랩임을 확인했다.
- 일반 조명 15개와 중앙 대형 조명 1개, 벽·기둥 `(0.56, 0.57, 0.56)`, 기존 바닥·흰색 천장을 함께 확인했다.
- USD reference 81개가 모두 상대 경로로 해석됐고, composed prim 71개와 `2250 × 1425` 미리보기를 검증했다.
