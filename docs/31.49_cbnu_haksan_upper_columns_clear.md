# 31.49 2층 중앙 기둥 3개 제거

- 수정일: 2026-09-18 (Asia/Seoul)
- 요청: 2층 기둥 3개 제거.
- 실행 파일: [2층 Stage](../worlds/cbnu_haksan_2f_building/cbnu_haksan_2f_building.usda)
- 이전 수정: [31.48 엘리베이터 문 복원](31.48_cbnu_haksan_upper_elevators_restore.md)

[update_cbnu_haksan_two_floor.py](../scripts/update_cbnu_haksan_two_floor.py)의 `main()`에서 `/World/Floor_02/Columns/Column_01`, `Column_02`, `Column_03`에 `active = false`를 적용했다. 기둥 형상과 하위 충돌 객체가 함께 제거된다. 1층 중앙 기둥과 정면 창 사이의 측면 기둥 2개는 유지한다.

[test_two_floor_world.py](../scripts/test_two_floor_world.py)의 `main()`에 세 제거 경로를 추가해 활성 Stage에 하위 객체가 남지 않는지 확인하고, 1층 형상은 원본과 비교한다. README에 현재 상태를 기록했다.

검증:

```bash
python3 scripts/update_cbnu_haksan_two_floor.py
./scripts/test_world_with_isaac_usd.sh scripts/test_two_floor_world.py
git diff --check
```

- Stage 합성, 2층 기둥 3개 비활성화 및 1층 형상 보존: PASS.
- 엘리베이터 문을 포함한 공통 형상 160개 위치·재질 비교: PASS.
- 창문·정면 측면 기둥·조명·층간 연결부 보존 및 이전 제거 대상 비활성화: PASS.
- Isaac Sim GUI 실시간 반영과 실제 로봇 주행은 미확인.
