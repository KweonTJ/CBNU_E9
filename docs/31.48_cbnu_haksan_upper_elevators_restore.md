# 31.48 2층 엘리베이터 문 복원

- 수정일: 2026-09-18 (Asia/Seoul)
- 요청: 엘리베이터 문을 원래대로 복원.
- 실행 파일: [2층 Stage](../worlds/cbnu_haksan_2f_building/cbnu_haksan_2f_building.usda)

[update_cbnu_haksan_two_floor.py](../scripts/update_cbnu_haksan_two_floor.py)의 `main()`에서 2층 `Architecture/ElevatorDoor_01`, `ElevatorDoor_02`의 비활성화 override를 제거했다. 원본 reference의 문·문틀·부속물과 재질이 그대로 복원된다. 문 배치 원점의 월드 좌표는 각각 `(22.8392, 15.2, 3.2)`, `(22.8392, 18.8, 3.2)`m다.

이전에 제거한 2층 일반 문·가구·택배·ATM·디스플레이·안내판은 제거 상태를 유지한다. 1층은 수정하지 않았다.

[test_two_floor_world.py](../scripts/test_two_floor_world.py)의 `main()`에서 두 엘리베이터를 제거 예외 목록에서 제외해 원본 대비 2층 높이 이동·형상·재질·schema 비교 대상에 다시 포함했다. README와 구조도 설명도 갱신했다.

검증 명령:

```bash
python3 scripts/update_cbnu_haksan_two_floor.py
./scripts/test_world_with_isaac_usd.sh scripts/test_two_floor_world.py
git diff --check
```

- Stage 합성 및 엘리베이터를 포함한 공통 형상 163개 비교: PASS.
- 이전 제거 대상 비활성화, 1층 객체 보존, 정면 창·조명·물리 객체 검사: PASS.
- GUI 실시간 반영과 실제 엘리베이터 동작은 미확인. 이번 수정은 기존 닫힌 문 모델의 복원이다.
