# CBNU 학연산 벽·기둥 추가 명도 조정 및 로비 조명 보강 기록

- 수정 날짜: 2026-09-04 (Asia/Seoul)
- 기준 월드: `worlds/cbnu_haksan_1f_corridor/cbnu_haksan_1f_corridor.usda`
- 목적: 벽·기둥과 회색 포스터의 명도 차이를 키우고, 어두워진 표면에 맞춰 메인 로비 조명을 소폭 보강한다.

## 변경 내용

- 벽·기둥 재질을 `(0.66, 0.67, 0.66)`에서 `(0.56, 0.57, 0.56)`으로 한 단계 더 어둡게 조정했다.
- 포스터 색상 `(0.70, 0.72, 0.74)`, Bala White 바닥과 흰색 천장은 변경하지 않았다.
- 일반 천장 패널 조명을 12개에서 15개로 늘렸다.
- 추가 위치는 메인 로비의 기존 빈 구간인 `(19.0, 12.0)`, `(28.8, 12.0)`, `(29.0, 6.0)`이며 모두 `z=2.96m`, `yaw=90°`다.
- 각 추가 패널은 기존 일반 패널 asset과 `RectLight intensity=8000` 설정을 그대로 사용한다.
- 중앙 대형 조명 1개와 DomeLight 설정은 유지했다.

## 검증

```bash
cd /home/a/Isaac_Worlds
python3 scripts/update_cbnu_haksan_ceiling.py
MPLCONFIGDIR=/tmp/cbnu_matplotlib python3 scripts/render_cbnu_haksan_preview.py
python3 scripts/validate_cbnu_haksan_detail.py
./scripts/test_world_with_isaac_usd.sh
```

- 상세 정적 검사: PASS
- Isaac Sim USD 24.05 composed Stage 검사: PASS
- 일반 조명 15개와 중앙 대형 조명 1개가 로드되며, 추가 조명 3개의 pose가 설정값과 일치함을 확인했다.
- 벽·기둥의 유효 재질이 `WallColumnLightGray`이고 바닥·천장 재질은 유지됨을 확인했다.
- USD reference 79개가 모두 상대 경로로 해석됐고, composed prim 65개와 `2250 × 1425` 미리보기를 검증했다.
