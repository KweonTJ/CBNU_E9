# CBNU 학연산 전시벽 비율·기둥 정렬 변경 기록

> 이후 전시벽 세로 비율·부상 높이·검정 배경·베젤·돌출 깊이 수정은 `31.7_cbnu_haksan_display_wall_compact_frame.md`를 기준으로 한다.

- 수정 날짜: 2026-09-01 (Asia/Seoul)
- 기준 월드: `worlds/cbnu_haksan_1f_corridor/cbnu_haksan_1f_corridor.usda`
- 목적: 코너 전시벽을 바닥에서 띄우고 긴 왼쪽 5개·짧은 오른쪽 3개 구성으로 수정하며 메인 기둥 3개를 로비 방향으로 당긴다.

## 디지털 전시벽 수정

- root Z를 `0.28m`로 올려 본체와 collision이 바닥에 닿지 않도록 했다.
- asset 높이 `2.35m`를 유지해 월드 상단은 `2.63m`다.
- 왼쪽 `SideSection` 길이를 `4.5m`, 오른쪽 `FrontSection` 길이를 `2.5m`로 변경했다.
- 왼쪽에는 `Display_04`–`08` 5개, 오른쪽에는 `Display_01`–`03` 3개를 배치했다.
- 모든 display는 공용 `digital_display_panel.usda`를 scale 없이 reference한다.
- 모든 display 외곽 크기는 `0.62 × 1.40m`로 동일하다.
- MainBody/HeaderUnified의 L footprint, frame, sign bar와 collision box를 새 좌우 길이에 맞췄다.
- screen의 `0.009m` recess와 low-emission 재질은 유지했다.

## 메인 기둥 수정

세 기둥을 X 간격을 유지한 채 로비 방향인 `-Y`로 각각 `0.30m` 이동했다.

| 기둥 | 새 중심 `(x, y)` |
| --- | --- |
| `Column_01` | `(21.7739, 10.3812)` |
| `Column_03` | `(26.43765, 10.38845)` |
| `Column_02` | `(31.1014, 10.3957)` |

- 각 단면 `1.2 × 1.2m`와 높이 `3.0m`는 유지했다.
- `Column_03`은 두 바깥 기둥의 정확한 중점을 유지한다.
- `Sofa_U_Column_02`는 furniture layout을 재생성해 Column 2와 함께 `-Y 0.30m` 이동했다.
- U형 소파와 Column 2의 clearance `0`은 유지했다.

## 재생성 방법

```bash
cd /home/a/Isaac_Worlds
python3 scripts/update_cbnu_haksan_architecture.py
python3 scripts/update_cbnu_haksan_furniture.py
MPLCONFIGDIR=/tmp/cbnu_matplotlib python3 scripts/render_cbnu_haksan_preview.py
```

## 검증 결과

- `python3 scripts/validate_cbnu_haksan_detail.py`: PASS
- `./scripts/test_world_with_isaac_usd.sh`: USD 24.05 composed Stage PASS
- display wall floor clearance: `0.28m`
- 왼쪽: `4.5m`, 동일 display 5개
- 오른쪽: `2.5m`, 동일 display 3개
- display 공통 크기: `0.62 × 1.40m`
- 메인 기둥: 3개 모두 `-Y 0.30m`, 동일 X 간격 유지
- active USD reference: 64개, 모두 상대경로이며 대상 존재
- composed 필수 prim: 48개
- architecture preview: `2250 × 1425`

## 확인 대기

Isaac Sim GUI에서 부상 높이, 왼쪽/오른쪽 비율과 기둥 전방 이동량의 시각적 적합성은 아직 확인하지 않았다.
